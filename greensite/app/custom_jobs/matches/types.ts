export { MATCH_EXPIRY_DAYS } from '../types'
export type { MatchedJob } from '../types'
export { mergeMatchedJobs, errorMessage } from '../types'

export interface StatCardData {
  label: string
  value: string
  color: string
}